- Stopped stripping heading font-size styles from the richtext HTML paste custom field so Google Docs heading sizing is preserved.
- Added a one-time blog backfill script with dry-run and apply modes to restore missing heading font-size styles in older posts.
- Updated the one-time backfill script teardown handling to ignore a known post-update Strapi shutdown abort so successful runs do not fail.
